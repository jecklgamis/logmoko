name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Create source archive
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          git archive --format=tar.gz --prefix=logmoko-${VERSION}/ HEAD \
            -o logmoko-${VERSION}.tar.gz
          git archive --format=zip --prefix=logmoko-${VERSION}/ HEAD \
            -o logmoko-${VERSION}.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            logmoko-*.tar.gz
            logmoko-*.zip
          generate_release_notes: true
